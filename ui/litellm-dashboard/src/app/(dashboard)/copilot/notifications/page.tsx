import { CopilotControlView } from "../../copilot-control/page";

export default function CopilotNotificationsPage() {
  return <CopilotControlView routeTab="notifications" syncWithQuery={false} />;
}
